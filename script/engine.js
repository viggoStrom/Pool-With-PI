/** @type {HTMLCanvasElement} */


class engine {
    constructor(canvas, ...objects) {
        this.canvas = canvas
        this.objects = objects
    }

    addVelocity = () => {
        this.objects.forEach(object => {
            object
        });
    }

    update = () => {

    }
}